<form method="post" action="{{ path('admin.taxes.delete', {'id': taxe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxe.id) }}">
	<button class="btn">Delete</button>
</form>
